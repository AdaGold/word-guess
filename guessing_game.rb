def GuessingGame

  def initialize(name)
    @name = name
    @answer = ["z", "e", "b", "r", "a"]
    @num_guess = 10
    @wr
    @art = (asci art)
    puts @art
  end

  def compare?
    @answer.include?guess
  end

  def method to ask user
    while @num_guess != 0
      puts "Guess a letter!"
      @guesses = []
      guess = gets.chomp.to_s
      @guesses << guesses

      if compare? == true






    end
  end
end
